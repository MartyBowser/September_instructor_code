package com.techelevator.avengers;

public interface Flyable {

    // by default, all methods in an interface
    // are public. We can declare them as public
    // but it's redundant
    public void fly();
}
